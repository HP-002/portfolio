import styles from './About.module.css'

export default function About() {
  return (
    <section className={styles.about}>
      {/* TODO: build creative About (includes collapsed publications link) */}
    </section>
  )
}
