import styles from './Skills.module.css'

export default function Skills() {
  return (
    <section className={styles.skills}>
      {/* TODO: build creative Skills (tag grid / visual layout) */}
    </section>
  )
}
