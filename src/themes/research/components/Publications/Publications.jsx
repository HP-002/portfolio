import styles from './Publications.module.css'

export default function Publications() {
  return (
    <section className={styles.publications}>
      {/* TODO: build research Publications (full citation style) */}
    </section>
  )
}
